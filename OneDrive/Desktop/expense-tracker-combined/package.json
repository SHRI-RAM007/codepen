{
  "name": "expense-tracker-combined",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "cd client && npm install",
    "client:dev": "cd client && vite",
    "client:build": "cd client && vite build",
    "build": "npm run client:build",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "vite": "^5.3.4"
  }
}